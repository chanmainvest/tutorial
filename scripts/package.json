{
  "name": "chanma-tutorial-build-scripts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Node-based asset generators for the Chanma Investment Tutorial. Used only to prebuild the chatbot embedding cache; the website build itself (build.py) stays Python-only.",
  "scripts": {
    "build:embeddings": "node build_chatbot_embeddings.mjs"
  },
  "devDependencies": {
    "@huggingface/transformers": "^4.0.0"
  }
}
