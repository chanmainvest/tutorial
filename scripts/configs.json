{
  "_comment": "LLM + translation configuration for scripts/translate-batch.py. Set 'provider' to anthropic | openai | gemini | copilot and the script reads the matching block under 'providers'. The 'copilot' provider routes through the GitHub Models inference API and uses your GitHub Copilot subscription tokens (auth via GITHUB_TOKEN env var, e.g. from `gh auth token`). CLI flags (--provider, --model, --locale, --file, --force, --dry-run) override these defaults.",

  "provider": "anthropic",
  "locales": ["hk", "tw", "cn"],
  "source_dir": "course",
  "output_dir": null,
  "file_pattern": null,
  "force": false,
  "dry_run": false,
  "skip_youtube_section": false,

  "providers": {
    "anthropic": {
      "model": "claude-sonnet-4-6-20250514",
      "max_tokens": 65536,
      "thinking_budget_tokens": 8000,
      "thinking_effort": "medium",
      "api_key_env": "ANTHROPIC_API_KEY",
      "input_cost_per_mtok": 3.0,
      "output_cost_per_mtok": 15.0
    },
    "openai": {
      "model": "gpt-5.4-mini",
      "max_output_tokens": 65536,
      "thinking_effort": "medium",
      "api_key_env": "OPENAI_API_KEY",
      "input_cost_per_mtok": 0.25,
      "output_cost_per_mtok": 2.0
    },
    "gemini": {
      "model": "gemini-3.1-flash",
      "max_output_tokens": 65536,
      "thinking_budget_tokens": 8000,
      "thinking_effort": "medium",
      "api_key_env": "GEMINI_API_KEY",
      "input_cost_per_mtok": 0.15,
      "output_cost_per_mtok": 0.6
    },
    "copilot": {
      "_comment": "GitHub Copilot CLI direct invocation (--prompt non-interactive mode). Requires `copilot` CLI on PATH. Uses your Copilot subscription \u2014 no separate API key needed. Set cli_path to override which binary is used.",
      "model": "claude-sonnet-4.6",
      "timeout_s": 7200,
      "cli_path": "copilot",
      "input_cost_per_mtok": 0.0,
      "output_cost_per_mtok": 0.0
    }
  }
}
